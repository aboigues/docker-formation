# TP3 — docker-compose.yml à compléter (la même stack que le TP5).
# Pas de clé "version:" : obsolète depuis Compose v2.

services:
  mysql:
    # TODO 1 : image MySQL 8.4
    image: ______
    volumes:
      # TODO 2 : monter le volume nommé db-data sur le répertoire de données MySQL
      - ______:/var/lib/mysql
    environment:
      # TODO 3 : les 4 variables d'environnement de MySQL
      MYSQL_ROOT_PASSWORD: rootsecret
      MYSQL_DATABASE: ______
      MYSQL_USER: ______
      MYSQL_PASSWORD: ______

  wordpress:
    # TODO 4 : image WordPress durcie (telemachlearning/wordpress:6.8-php8.3-apache)
    image: ______
    ports:
      # TODO 5 : publier le port 80 du conteneur sur le 8083 de l'hôte
      - "______"
    environment:
      # TODO 6 : WORDPRESS_DB_HOST doit valoir le NOM DU SERVICE de la base
      WORDPRESS_DB_HOST: ______
      WORDPRESS_DB_NAME: wordpress
      WORDPRESS_DB_USER: wpuser
      WORDPRESS_DB_PASSWORD: wpsecret
    # TODO 7 : démarrer wordpress APRÈS mysql
    depends_on:
      - ______

# TODO 8 : déclarer le volume nommé db-data
volumes:
  ______:
