# Données et artefacts générés par les TP
**/backups/
**/data/
*.tar
*.tar.gz
*.tar.xz
**/certs/*.crt
**/certs/*.key
**/auth/htpasswd
.env
!.env.example
# Exception : le .env de la solution de référence (mots de passe FICTIFS) est
# committé car la CI en a besoin pour lancer la stack. Les participants, eux,
# créent leur propre .env dans starter/ (toujours ignoré).
!tp6-compose-avance/solution/.env

# Système
.DS_Store
*.log
